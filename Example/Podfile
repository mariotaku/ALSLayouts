platform :ios, '9.0'
use_frameworks!

target 'ALSLayouts_Example' do
  pod 'ALSLayouts', :path => '../'

  target 'ALSLayouts_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
  end
end
